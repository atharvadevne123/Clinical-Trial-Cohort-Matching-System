[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: mark test as slow-running
    integration: mark test as integration test
asyncio_mode = auto
